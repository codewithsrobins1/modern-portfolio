import about from '../schemas/about';
import workExperience from '../schemas/workExperience';
import project from '../schemas/project';

export const schema = {
  types: [about, workExperience, project],
};
